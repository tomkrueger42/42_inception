version: '3.9'

services:
  nginx:
    build: requirements/nginx
    container_name: nginx
    depends_on:
      - mariadb # depends_on can be done with a health check
    ports:
      - 8080:80

  mariadb:
    build: requirements/mariadb
    container_name: mariadb
    ports:
      - "443:443"
      - "80:80"
    volumes:
      - db_data:/var/lib/mysql
    networks:
      - inception_backend
    env_file:
      - .env

volumes:
  db_data:
    driver: 'local'
    driver_opts:
      o: 'bind'
      type: 'none'
      device: '/home/tkruger/data/mysql'

networks:
  inception_backend: